import Spinner from '@glint/ui/spinner';

const PageSpinner: React.FC = () => {
    return (
        <div className="flex h-full w-full items-center justify-center h-20">
            <Spinner />
        </div>
    );
};

export default PageSpinner;
