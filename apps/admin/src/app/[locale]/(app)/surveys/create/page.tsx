import Container from '@glint/ui/container';
import Form from './form';

const Page: React.FC = () => {
    return (
        <Container>
            <Form />
        </Container>
    );
};

export default Page;
